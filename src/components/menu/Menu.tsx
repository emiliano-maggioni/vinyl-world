import classes from "./Menu.module.css";

const Menu = () => {
    return (
        <menu className={classes.container}>
           <div className={classes.content}>
                        MENUUUU
           </div>
        </menu>
    );
}

export default Menu;